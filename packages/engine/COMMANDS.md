# Solitaire – Handy Commands

## Repo layout

- Repo root (monorepo):
  `C:\Users\joshf\solitaire`

- Engine package:
  `C:\Users\joshf\solitaire\packages\engine`

Tip: In VS Code, you can right-click a folder → **Open in Integrated Terminal**
to start a terminal whose working directory is that folder.

---

## From repo root

```bash
# Install all dependencies (monorepo)
pnpm install
